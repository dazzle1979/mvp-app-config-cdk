name: Repository dispatch
on:
  repository_dispatch:
    types: [test]
jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - name: Print Outputs
        run: |
          echo Message
